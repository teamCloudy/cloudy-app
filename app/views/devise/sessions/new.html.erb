<h2>Log in</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :email, :class => "form-element"%><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", :class => "form-element" %>
  </div>

  <div class="field">
    <%= f.label :password, :class => "form-element" %><br />
    <%= f.password_field :password, autocomplete: "current-password", :class => "form-element" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me, :class => "form-element" %>
      <%= f.label :remember_me, :class => "form-element" %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "Log in" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
